"""Откуда у бизнеса взялся объект — чтобы у актива было происхождение, а не только сумма.

ЗАЧЕМ. Баланс — две стороны одного имущества: слева «что есть», справа «откуда взялось». Обе
всегда равны, потому что вещь без происхождения не появляется. Покупка за деньги фирмы правую
сторону не трогает: деньги уменьшились, объект появился, итог тот же. А вот объект, за который
фирма не платила — собственник принёс свой ноутбук, мангал, стеллаж, — увеличивает актив, ничего
не уменьшая. И тогда справа обязана появиться запись: чьи это деньги.

Вариантов у неё ровно три, и разница между ними живая:

* ``owner_contribution`` — собственник отдал вещь бизнесу. Растёт его доля; забрать потом можно,
  но это будет изъятие капитала;
* ``owner_loan`` — бизнес остаётся должен. Растёт кредиторка; вернуть можно деньгами, и возврат
  не будет ни доходом, ни расходом — просто гасится обязательство;
* ``donation`` — безвозмездно со стороны, не от собственника. Это доход, а не капитал.

``purchase`` ставится сам и вопросов не задаёт: оценка «по платежу» уже означает, что заплатила
фирма.

ПОЧЕМУ КОЛОНКА, А НЕ ТАБЛИЦА ВЗНОСОВ. В ``app-spec`` описана полноценная ``owner_contribution``
с владельцем, периодом и качеством источника — но её нет, как нет и таблицы ``owner``, а модуля
баланса нет вовсе (страница «Баланс» — заглушка). Строить контур УФД ради одного факта рано.
Задача сейчас другая и узкая: не потерять факт в момент, когда человек его ещё помнит. Сумма у
объекта уже есть (``initial_cost``), дата тоже — не хватало только происхождения. Когда дойдёт
до баланса, из этих карточек соберётся правая сторона; из ничего она не соберётся никогда.

NULL — честное «не указано», и оно останется у 149 карточек описи 2026 (2 313 150 ₽). Проставить
им ``owner_contribution`` скопом было бы удобно и неправильно: часть из них могла быть куплена
фирмой, часть принесена, и знает это только владелец. Разметку он сделает сам.

Revision ID: 0232_asset_acquisition_source
Revises: 0231_repair_articles_named
Create Date: 2026-08-01
"""

from __future__ import annotations

import sqlalchemy as sa

from alembic import op

revision = "0232_asset_acquisition_source"
down_revision = "0231_repair_articles_named"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "fixed_asset", sa.Column("acquisition_source", sa.String(length=24), nullable=True)
    )
    op.create_check_constraint(
        "ck_fixed_asset_acquisition_source",
        "fixed_asset",
        "acquisition_source IS NULL OR acquisition_source IN "
        "('purchase','owner_contribution','owner_loan','donation')",
    )
    # Карточки, заведённые контуром покупки, происхождение имеют по определению: за них
    # заплатила фирма, и оценка у них «по платежу». Проставляем, чтобы «не указано» осталось
    # только там, где вопрос действительно открыт.
    op.execute(
        "UPDATE fixed_asset SET acquisition_source = 'purchase' WHERE valuation_basis = 'payment'"
    )


def downgrade() -> None:
    op.drop_constraint("ck_fixed_asset_acquisition_source", "fixed_asset", type_="check")
    op.drop_column("fixed_asset", "acquisition_source")
