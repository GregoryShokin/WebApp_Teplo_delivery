"""Информационный закрывающий документ: пришёл, зарегистрирован, учёт не двигает.

ЗАЧЕМ. У контрагента с договором услуги источник истины — договор, а не бумага. ИП Наумченко
берёт 3 000 ₽ в месяц, система начисляет долг сама, и это её единственный расход за месяц.
Если такой контрагент ВДРУГ пришлёт акт (28.07.2026 она прислала — вместе со счётом, на том
же листе), обычная обработка провела бы его как настоящий: отменила бы наше начисление,
завела расход по документу и перегасила дебиторку. Итог — учёт скачет от бумаги, которую мы
не запрашивали, а суммы за месяц могут разойтись с договором.

Решение владельца (01.08.2026): по договорному контрагенту документ принимается, но НИ НА ЧТО
НЕ ВЛИЯЕТ. Он виден в реестре и в сверке — заявленное расхождение с договором надо замечать,
а не прятать, — но расхода не создаёт, дебиторку не гасит и начисление не замещает.

Флаг именно на документе, а не вывод на лету: режим договора могут выключить или договор
закончится, и тогда прошлые документы задним числом превратились бы в действующие, переписав
закрытые месяцы.

Revision ID: 0235_informational_doc
Revises: 0234_service_agreement
Create Date: 2026-08-01
"""

from __future__ import annotations

import sqlalchemy as sa

from alembic import op

revision = "0235_informational_doc"
down_revision = "0234_service_agreement"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "supplier_invoice",
        sa.Column(
            "informational",
            sa.Boolean(),
            nullable=False,
            server_default=sa.text("false"),
        ),
    )


def downgrade() -> None:
    op.drop_column("supplier_invoice", "informational")
