# Журнал фиксаций

## 2026-08-03 — FEAT-003 закрыт: «Страница на оплату» стала вкладкой «Платежей»
- статус FEAT-003 draft → done: «Активные» заменены журналом входящих счетов, добавлена вкладка «Новые контрагенты» (очередь разбора карточек из ЭДО + документы без ИНН), вкладки переведены на пути, `/payment-page` → редирект, пункт меню убран
- разбор контрагента из ЭДО: карточка открывается прямо во вкладке, новый `POST /sbis/counterparties/{id}/reroute` убирает строку из «новых» сразу, а не следующим часовым синком
- починены права: `/payment-page/*` принимал только `counterparties.read`, из-за чего finance-роль видела пункт меню и получала тихий 403 на списке счетов; тест закрывает все три роута контура сразу
- у `GET /finance/payments` появился первый HTTP-тест — витрина, питающая страницу и FAB-модалку, не была покрыта вовсе
- решения владельца: вкладка «Вся история» остаётся агрегатором, FAB сохранена, кнопка разбора — только по праву ADMIN

## 2026-07-09 — Бэклог: пополнения, партнёры, поставщики-НДС, «Платежи»
- создано: FEAT-001 (пополнение счетов Сейф/Касса + реестр партнёров и выплата изъятий, кейс «Алиса»), FEAT-002 (поставщики с обязательным НДС по товарам в накладных), FEAT-003 (страница «Платежи» — перенос «Страницы на оплату» в Финансы + агрегатор платежей и статусов черновиков)
- backlog: +1 пункт (перевод с Торговой кассы на Сейф)
- заведена структура project-scribe в docs/ (INDEX, JOURNAL, BACKLOG, features/); legacy-документы docs/access-rights-matrix.md и docs/runbook-payroll-manager.md оставлены как есть
